// Tester code for an oDeck object
// oDeck should not be created in oBattle, needs to persist beyond combat
fillDeck(oDeck, global.playerData.startDeck, global.playerData.maxHandSize);
oDeck.initDeck();